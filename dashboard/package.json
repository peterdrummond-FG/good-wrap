{
  "name": "good-wrap-dashboard-poc",
  "private": true,
  "version": "0.0.1",
  "description": "Temporary standalone POC dashboard for good-wrap (Stage 4). Not the final UI — this gets rewritten to live inside Peter's existing hub app once the shape of the pages is agreed on. See Project-Handoff-Brief.md Section 8.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@quasar/extras": "^2.0.2",
    "quasar": "^2.21.3",
    "vue": "^3.5.39",
    "vue-router": "^5.1.0"
  },
  "devDependencies": {
    "@quasar/vite-plugin": "^1.12.0",
    "@vitejs/plugin-vue": "^6.0.8",
    "sass-embedded": "^1.100.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.4",
    "vue-tsc": "^3.3.7"
  }
}
